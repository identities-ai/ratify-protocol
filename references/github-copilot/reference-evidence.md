# Reference evidence

Generated by `npm run check` against published dependencies pinned in `package-lock.json`.

## Required outcomes

| Case | Receiver decision | Protected handler count |
| --- | --- | ---: |
| Valid staging deployment | ALLOW | 1 |
| Production path | DENY | 0 |
| Changed artifact after challenge | DENY | 0 |
| Different repository | DENY | 0 |
| Replayed proof | DENY | unchanged at 1 |
| Revoked delegation | DENY | 0 |
| Untrusted human root | DENY | 0 |

The gate requires TypeScript compilation and exactly seven passing tests with no failures or skips.

## Native GitHub Copilot CLI execution

Executed 2026-08-14 with GitHub Copilot CLI 1.0.80 using the local
`--plugin-dir` development path. Copilot loaded `ratify-authority`, selected the
`deploy_service` MCP tool, and received:

```json
{
  "allowed": true,
  "reason": "authorized",
  "handler_invocations": 1,
  "receipt": {
    "scope": "custom:github:deploy",
    "resource": "github:identities-ai/copilot-authority-demo",
    "path": "/services/payments/environments/staging",
    "invocation_id": "copilot-native-001"
  }
}
```

This proves native plugin discovery, skill and MCP configuration loading, MCP
tool selection, adapter proof presentation, independent HTTP verification, and
protected-handler invocation after allow.

## Clean distribution execution

The plugin was rebuilt with `npm run build:plugin`, then only `plugin.json`,
`.mcp.json`, `skills/`, and `plugin-runtime/` were copied into a new temporary
directory. The copy contained no `node_modules` and no TypeScript source.

Copilot CLI 1.0.80 loaded `ratify-authority` from that directory and completed
invocation `clean-release-001`. The receiver returned `allowed: true` with
`handler_invocations: 1`. This verifies that a repository-installed plugin does
not depend on the development worktree.
