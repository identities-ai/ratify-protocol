# Controller component

Laptop or cloud controller. This component provides deterministic test fixtures
first, then optional agent adapters. It must
never have a direct actuator path that bypasses the Raspberry Pi verifier.
